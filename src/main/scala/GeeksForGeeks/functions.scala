package GeeksForGeeks


object functions {

  def main(args: Array[String]) {

   /*var ex1 = (x:Int) => x + 2
    var ex2 = (x:Int,y:Int) => x + y
    println(ex1(4))
    println(ex2(3,4))*/
   /* var a = "Hi there"
    var b = "Laxman"
    var c = a.concat(b)
    var Len1 = a.length()
    var Len2 = b.length()
    //println("the length of the string1 is "+Len1)
    //println("the length of the string2 is "+Len2)
    println(a)
    println(b)
    println(c)*/

    /*var a = 2
    var b = 3
    println(s"sum of a and b is ${a+b}")*/

    var x = 20
    println(f"x \nis $x%04d")
  }
}
